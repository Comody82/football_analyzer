# Football Analyzer - dipendenze
# Su Python 3.10 Windows YOLOX richiede onnx/onnxruntime senza wheel: installare YOLOX a parte.
# 1) Assicurati di avere CMake installato (winget install Kitware.CMake)
# 2) pip install PyQt5 opencv-python numpy scikit-learn torch torchvision
# 3) pip install yolox --no-deps
# 4) pip install pycocotools loguru ninja scikit-image tabulate tensorboard thop tqdm
PyQt5>=5.15.0
opencv-python>=4.8.0
numpy>=1.24.0
scikit-learn>=1.3.0
torch>=1.10.0
torchvision>=0.11.0
yolox>=0.3.0
psutil>=5.9.0
requests>=2.28.0

# Per build exe (Fase 3)
# pyinstaller>=6.0

# Opzionale: server mock API per test client Fase 4
# pip install flask && python -m api.mock_server
# flask>=2.0.0

# Opzionale: export report PDF (Fase 8)
# pip install reportlab
# reportlab>=3.6.0
